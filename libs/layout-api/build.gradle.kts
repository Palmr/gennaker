plugins {
    id("java-conventions")
    id("publishing-conventions")
}

description = "Layout API for describing how custom types map to Gennaker wire fields"

repositories {
    mavenCentral()
}

dependencies {
}
